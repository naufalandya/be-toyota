global:
  scrape_interval: 15s # Scrape metrics every 15 seconds.

scrape_configs:
  - job_name: "app"
    static_configs:
      - targets: ["app:3001"] # Replace with the app container's service name and port.
